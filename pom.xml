<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>hops.io.kafka</groupId>
  <artifactId>hops-kafka-authorizer</artifactId>
  <version>5.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Kafka 4.x artifacts target Java 17; the broker itself also runs on 17+. -->
    <maven.compiler.release>17</maven.compiler.release>
    <kafka.version>4.3.0</kafka.version>
    <mysql.version>8.0.33</mysql.version>
    <guava.version>32.1.3-jre</guava.version>
    <hikariCP.version>2.6.0</hikariCP.version>
    <javatuples.version>1.2</javatuples.version>
    <junit.version>5.9.2</junit.version>
    <mockito.version>4.3.1</mockito.version>
    <slf4j.version>1.7.36</slf4j.version>
  </properties>
  <dependencies>
    <!--
      kafka-clients carries every Kafka API this plugin touches: the
      org.apache.kafka.server.authorizer.* interfaces, org.apache.kafka.common.*, and
      DefaultKafkaPrincipalBuilder. Kafka 4.0 dropped Scala 2.12, so depending on the
      Scala kafka_<scala.version> artifact would tie us to Kafka's Scala version for no
      benefit — nothing here imports kafka.* or scala.*.
    -->
    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-clients</artifactId>
      <version>${kafka.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>${mysql.version}</version>
    </dependency>
    <!--
      Shaded in and relocated (see the shade plugin config below), not provided.
      Strimzi's Kafka image does carry guava, but only incidentally: it arrives as a
      transitive of io.grpc:grpc-netty-shaded in kafka-thirdparty-libs, and as the
      -android flavour at a different major (33.5.0-android on Strimzi 1.1.0) than we
      compile against. Relying on that is fragile - it disappears the day Strimzi drops
      the OpenTelemetry/gRPC exporter stack - and bundling it unrelocated would put two
      different com.google.common.cache implementations on the broker classpath, where
      resolution order decides which one wins. Relocating gives us a private copy with
      neither problem.
    -->
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
      <version>${guava.version}</version>
    </dependency>
    <dependency>
      <groupId>com.zaxxer</groupId>
      <artifactId>HikariCP</artifactId>
      <version>${hikariCP.version}</version>
    </dependency>
    <dependency>
      <groupId>org.javatuples</groupId>
      <artifactId>javatuples</artifactId>
      <version>${javatuples.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>${mockito.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <!--
          Pinned: with no version Maven falls back to 3.1, which predates
          maven.compiler.release and silently compiles with source/target 5.
        -->
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <!-- JUnit 5 requires Surefire version 2.22.0 or higher -->
        <version>3.0.0-M8</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <!--
          3.x required: 2.4.3 bundles an ASM that cannot parse Java 17 class files, so
          relocation fails with an opaque IllegalArgumentException.
        -->
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>                        
              <goal>shade</goal>
            </goals>
            <configuration>
              <artifactSet>
                <excludes>
                  <!-- Don't include protobuf classes -->
                  <exclude>com.google.protobuf:protobuf-java</exclude>
                </excludes>
              </artifactSet>
              <relocations>
                <!--
                  Keep our guava private. The Kafka broker classpath also has a guava
                  (transitive of grpc-netty-shaded in Strimzi's third-party libs, a
                  different major and the -android flavour); without relocation both
                  supply com.google.common.* and the winner depends on scan order.
                -->
                <relocation>
                  <pattern>com.google.common</pattern>
                  <shadedPattern>io.hops.kafka.shaded.com.google.common</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>com.google.thirdparty</pattern>
                  <shadedPattern>io.hops.kafka.shaded.com.google.thirdparty</shadedPattern>
                </relocation>
              </relocations>
              <transformers>
                <!-- add Main-Class to manifest file -->
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>io.hops.kafka.HopsAclAuthorizer</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>                
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>2.16</version>
        <executions>
          <execution>
            <id>validate</id>
            <phase>validate</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <suppressionsLocation>/tools/maven/suppressions.xml</suppressionsLocation>
          <suppressionsFileExpression>/checkstyle.suppressions.file</suppressionsFileExpression>
          <configLocation>/tools/maven/checkstyle.xml</configLocation>
          <logViolationsToConsole>true</logViolationsToConsole>
          <failsOnError>true</failsOnError>
          <consoleOutput>true</consoleOutput>
        </configuration>
      </plugin>
    </plugins>       
  </build>
</project>
